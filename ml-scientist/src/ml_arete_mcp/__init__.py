"""ml-arete-mcp — Loop 2, the recursive loop.

Improves the improver: meta-change proposals, admission-time
writable-boundary checks (ADR-0003), paired ancestral tournaments
under equal budget, governed meta-decisions, policy promotion and
rollback — with durable provenance throughout.
"""
